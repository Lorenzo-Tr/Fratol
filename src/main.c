#include "main.h"

int main(int argc, char **argv){
  unsigned int height;
  unsigned int length;
  char* name;
  if(argc != 3){
    printf("Invalid arguments\n");
    return(1);
  }
  height = atoi(argv[1]);
  weight = atoi(argv[2]);
  name = argv[3];

  return (0);
}